# Ecosystem - {PROJECT}

> Project profile. Fill once. Update only when stack/conventions change.
> This file answers: *"What is this project, and what should the AI assume?"*

## What this is

- **Type:** {code | content | research | design | docs | other}
- **One-line:** {ONE_LINE_DESCRIPTION}
- **Stage:** {prototype | active | maintenance | archived}

## Stack (skip sections that don't apply)

- **Language(s):** {e.g., TypeScript, Python, Rust, none}
- **Framework(s):** {e.g., Next.js 15, FastAPI, none}
- **Package manager:** {npm | pnpm | yarn | bun | pip | uv | cargo | go | none}
- **Runtime:** {Node 20, Python 3.12, etc.}
- **Database:** {Postgres | SQLite | none}
- **Deployment:** {Vercel | AWS | self-hosted | static | n/a}

## Commands (fill what applies)

- Test: `{TEST_CMD}`
- Build: `{BUILD_CMD}`
- Dev: `{DEV_CMD}`
- Lint: `{LINT_CMD}`
- Format: `{FORMAT_CMD}`
- Typecheck: `{TYPECHECK_CMD}`

## Conventions

- **Commits:** {Conventional Commits | team-specific format | none}
- **Branches:** {trunk-based | feature/* | git-flow}
- **Code style:** {Prettier defaults | ESLint config | PEP 8 | rustfmt}
- **Testing:** {required for new features | best-effort | none}

## Constraints

- {e.g., "Must support Node 18+", "No external API calls in client bundle", "Browser targets: last 2 versions"}

## Context

- **Owner / team:** {solo | team of N | client work for X}
- **Audience:** {internal | public | enterprise}
- **Sensitivity:** {public | private | contains secrets — see `.env.example`}

## Out of scope

- {Things you do NOT want the AI to suggest, refactor, or touch.}

---

*Read by `loadgame` and any agent starting a new session. Keep under 1 page.*
